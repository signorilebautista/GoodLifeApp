VITE_API_URL=http://localhost:3000

# Clave pública VAPID para notificaciones push (debe coincidir con VAPID_PUBLIC_KEY del backend).
# Sin esta variable, la sección de notificaciones queda oculta.
VITE_VAPID_PUBLIC_KEY=
